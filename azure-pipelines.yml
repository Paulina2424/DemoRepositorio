stages:
  - stage: Build
    jobs:
      - job: BuildMS
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - template: templates/npm-steps.yml
        steps:
          - script: ./gradlew build
            name: build
