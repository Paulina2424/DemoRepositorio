stages:
  - stage: Build
    jobs:
      - job: BuildMS
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - template: ci/build-all-steps.yml
            parameters:
              os: Linux
          - script: ./gradlew build
            name: build
