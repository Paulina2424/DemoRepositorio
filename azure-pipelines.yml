
variables:
  imageName: 'pipelines-javascript-docker'

stages:
  - stage: Build
    jobs:
      - job: BuildMS
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - script: chmod 777 gradlew
            name: permisos
          - script: ./gradlew build
            name: build
          - task: CopyFiles@2
            displayName: 'Copy scripts'
            inputs:
              SourceFolder: '$(system.defaultworkingdirectory)'
              contents: '**/*.jar'
              targetFolder: '$(Build.ArtifactStagingDirectory)'
          - task:  PublishBuildArtifacts@1
            inputs:
             pathPublish: '$(Build.ArtifactStagingDirectory)'
             artifactName: 'DemoRepositorio' 
             publishLocation: 'Container'
             storeAsTar: true
      
  - stage:	
    dependsOn: Build
    jobs:
      - job: Docker
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - download: current
            artifact: DemoRepositorio
          - script: |
              mkdir uncomprensse 
              tar -xvf /home/vsts/work/1/DemoRepositorio/DemoRepositorio.tar -C ./uncomprensse 
            name: 'descompresión'
            
         
          - script: ls -l  ./uncomprensse
         # - script: docker images
          #- script: docker run -dp 8080:8060 node:12
          #- script: docker ps 
         
              


